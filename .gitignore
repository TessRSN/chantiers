# Fichiers de travail locaux (ne pas push sur GitHub)
*.xlsx
*.pptx
.DS_Store
Thumbs.db

# Workspace de brainstorming visuel (mockups, sessions)
.superpowers/

# Workspace local Claude Code (worktrees, état de session)
.claude/

# Mails et communications (envois ciblés, pas de tracking sur le repo)
mails-comite-12juin/
mails-*/
